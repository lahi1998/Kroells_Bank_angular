import { TreepointAfterPipe } from './treepoint-after.pipe';

describe('TreepointAfterPipe', () => {
  it('create an instance', () => {
    const pipe = new TreepointAfterPipe();
    expect(pipe).toBeTruthy();
  });
});
